# Schemas

Schemas are deliberately human-readable first. Machine-readable JSON Schema
will be added once the first complete API record is imported and its fields have
been exercised against AOSP and device observations.
